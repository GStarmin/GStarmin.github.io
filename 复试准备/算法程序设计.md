## 常用算法
### 快排
```C
void QuickSort(ElemType A[],int low,int high)
{
    if(low<high>)
    // Partation()就是划分操作，将表A[low...high]划分为满足条件的上述两个子表
    int pivotpos=Partition(A,low,high);
    QuickSort(A,low,pivotpos-1);    //一次对两个表进行递归排序
    QuickSort(A,pivotpos+1,high);

}

int Partition(ElemTpye A[],int low, int high)
{
    while(low<high)
    {
        while(low<high && A[high]>= pivot)  
            high--;
        while(low<high && A[low]<= pivot)
            low++;
    }
    A[low]=pivot;
    return low;
}

```

### 二分查找/折半查找
```C++
int Binary_Search(SeqList L,ElemType key)
{
    int low =0 , high = L.TableLen - 1, mid;
    while(low <= high)
    {
        mid =(low+high)/2;
        if(L[mid]==key)
            return mid;
        else if(A[mid]<key)
            low=mid+1;
        else
            high=low-1;
    }
    return -1;
}
```

